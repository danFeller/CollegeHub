import styled from 'styled-components';

function EventHub(){
    return(
        <>
            <h1>A grid of modals showing all upcoming events will appear here chronologically, and you can filter out the events you are not attending, and filter between which events you are an attendee or organizer for. Event.js will be my modal component that will show the event name, the start time, the organizer name, and a color code representing </h1>

        </>
    )
}

export default EventHub;